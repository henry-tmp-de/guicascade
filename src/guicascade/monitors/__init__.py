"""监控层：读轨迹文本，给出「该不该升级」的信号。

三层实现，代表三种完全不同的思路：

    repeat   纯代码数重复      零参数、零延迟、完全可解释   <- 下界基线
    bert     149M 文本分类器   原方法给的，实测已失效（见下）
    prompt   让小模型打分      贵，但换环境不用重训

⚠️ 关于 `bert`：原方法发布的卡住监控器在本项目的实测中**没有判别力**
（桌面 AUC 0.608，安卓 AUC 0.423——后半段的数字比瞎猜还差，因为正常窗口
的均分 0.999 反而高于卡住窗口的 0.976）。里程碑那路是好的，跨环境能迁移
（0.782 -> 0.725）。完整的排除过程记在本地工程笔记里。

所以默认配置里卡住那一路用 `repeat`，不用 `bert`。保留 `bert` 的实现是为了
让"它不如纯代码"这个结论**可复现**——删掉它，这个结论就变成了一句口说无凭的话。
"""

from .base import Monitor
from .repeat import RepeatMonitor

__all__ = ["Monitor", "RepeatMonitor"]
